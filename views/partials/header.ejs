<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Creative Cardigans</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid d-flex align-items-center justify-content-between">
            
            <!-- Logo (Left) -->
            <a href="/" class="navbar-brand me-auto">
                <img src="/images/CC.png" alt="Creative Cardigans" height="50">
            </a>

            <!-- Search Bar (Center) -->
            <div class="search-container mx-auto">
                <form action="/items" method="GET" class="d-flex">
                    <input type="text" name="search" placeholder="Search..." 
                        class="search-bar form-control" 
                        value="<%= typeof searchTerm !== 'undefined' ? searchTerm : '' %>">
                    <button type="submit" class="btn btn-light mx-2">Search</button>
                </form>
            </div>

            <!-- Navigation Links (Right) -->
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link" href="/items">Browse</a></li>
                    <li class="nav-item"><a class="nav-link" href="/items/new">Sell</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Signup</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Login</a></li>
                </ul>
            </div>
        </div>
    </nav>
